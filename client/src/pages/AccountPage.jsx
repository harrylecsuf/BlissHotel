import React from 'react';

export default function AccountPage() {
const {user} = useContext(UserContext);
        if (!uder){}



    return (
        <div>
            <h2>Account Page {user.name} </h2>
            {/* Account details and logic */}
        </div>
    );
}

